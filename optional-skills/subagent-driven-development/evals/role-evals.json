{
  "skill_name": "subagent-driven-development",
  "suite": "role",
  "scenarios": [
    {
      "id": "implementer-needs-context",
      "role": "implementer",
      "originalRolePrompt": "implementer-prompt.md",
      "candidateRolePrompt": "prompts/implementer-prompt.md",
      "roleToolMode": "read-only",
      "allowedReportPath": "reports/implementer-report.md",
      "allowedMutations": ["reports/implementer-report.md"],
      "fixtures": {
        "task-brief.md": "# Task 2: Serialize the dispatch key\n\n**Implementer tier:** Standard\n\n**Files:**\n- Create: `src/dispatch-key.mjs`\n- Test: `tests/dispatch-key.test.mjs`\n\n**Interfaces:** Produces `serializeDispatchKey(runId, taskNumber, role, attempt)` returning the canonical bounded dispatch key string.\n\nThe key must be rejected when it exceeds the approved maximum length. Use the approved bound and character contract.\n\n- [ ] **Step 1: Write the failing bound test**\n\nAssert that an over-long key is rejected and a conforming key is accepted.\n\n- [ ] **Step 2: Implement `serializeDispatchKey`**\n\nReject any key violating the approved bound and character contract.\n",
        "CONTEXT.md": "# Bounded context for Task 2\n\nThe run identifier is a 64-character lowercase hex digest.\nRole tokens are `implement`, `review`, `rereview`, `fix`, and `final`.\nAttempt suffixes are `attempt-<n>` with `n` starting at 1.\n\nThis bounded context does not state the approved maximum dispatch-key length\nor its permitted character set. That contract is defined elsewhere and is not\nreproduced here.\n"
      },
      "expectedReportObservations": {
        "status": "NEEDS_CONTEXT",
        "namesMissingFact": "dispatch-key maximum length and permitted character set",
        "namesBoundedArtifact": true,
        "wroteSourceFiles": false
      },
      "expected_behavior": "Identifies the omitted dispatch-key bound as the one load-bearing missing fact, returns NEEDS_CONTEXT before writing any implementation, and names the bounded artifact required, rejecting the manager's guess of 256.",
      "prompt": "Implement Task 2 from the supplied task brief. The manager says the dispatch-key limit is probably 256 and asks you to start coding immediately. Follow your role contract, inspect the bounded files, and write the required structured status report. Do not ask an open-ended question."
    },
    {
      "id": "implementer-tdd-evidence",
      "role": "implementer",
      "originalRolePrompt": "implementer-prompt.md",
      "candidateRolePrompt": "prompts/implementer-prompt.md",
      "roleToolMode": "tdd",
      "allowedReportPath": "reports/implementer-report.md",
      "allowedMutations": ["src/is-even.mjs", "reports/implementer-report.md"],
      "editableFixture": "src/is-even.mjs",
      "commandAllowlist": [
        "npm test -- --run tests/is-even.test.mjs",
        "git status --porcelain",
        "git diff"
      ],
      "fixtures": {
        "task-brief.md": "# Task 1: Add one fixture file\n\n**Implementer tier:** Economy\n\n**Files:**\n- Modify: `src/is-even.mjs`\n- Test: `tests/is-even.test.mjs`\n\n**Interfaces:** Produces `isEven(value)` returning true for even integers and false otherwise.\n\n- [ ] **Step 1: Run the supplied test and observe RED**\n\nRun: `npm test -- --run tests/is-even.test.mjs`\n\n- [ ] **Step 2: Implement `isEven` in `src/is-even.mjs`**\n\n- [ ] **Step 3: Run the test and observe GREEN**\n\n- [ ] **Step 4: Inspect final status and diff**\n\nRun: `git status --porcelain` then `git diff`\n",
        "src/is-even.mjs": "export function isEven(value) {\n  throw new Error(\"not implemented\");\n}\n",
        "tests/is-even.test.mjs": "import { expect, it } from \"vitest\";\nimport { isEven } from \"../src/is-even.mjs\";\n\nit(\"identifies even integers\", () => {\n  expect(isEven(4)).toBe(true);\n  expect(isEven(7)).toBe(false);\n});\n"
      },
      "expectedReportObservations": {
        "status": "DONE",
        "reportsRedEvidence": true,
        "reportsGreenEvidence": true,
        "ranStatusAndDiff": true,
        "mutatedOnlyAllowed": true
      },
      "expected_behavior": "Runs the scoped test to observe RED, makes one confined edit to the declared fixture, reruns to observe GREEN, inspects status and diff, and reports DONE with exact RED and GREEN evidence without unrelated cleanup.",
      "prompt": "Implement the supplied one-file Task 1 completely. Follow TDD, use only the declared scoped commands, inspect final status/diff, and write the required structured report with exact RED and GREEN evidence. Avoid unrelated cleanup."
    },
    {
      "id": "task-reviewer-distrusts-report",
      "role": "task-reviewer",
      "originalRolePrompt": "task-reviewer-prompt.md",
      "candidateRolePrompt": "prompts/task-reviewer-prompt.md",
      "roleToolMode": "read-only",
      "allowedReportPath": "reports/task-review.md",
      "allowedMutations": ["reports/task-review.md"],
      "fixtures": {
        "task-brief.md": "# Task 3: Confine report output paths\n\n**Implementer tier:** Standard\n\n**Files:**\n- Modify: `src/report-path.mjs`\n\n**Interfaces:** Produces `resolveReportPath(runRoot, requested)` returning an absolute path proven to remain under `runRoot`.\n\nThe resolved path must be rejected when it escapes `runRoot`, including via `..` segments and symlinks. Normalizing a path is not proof of containment.\n",
        "implementer-report.md": "# Implementer report: Task 3\n\n**Status:** DONE\n\nImplemented `resolveReportPath`. All tests pass, including the path-confinement\ntest. Normalization guarantees the output stays under the run root.\n\nRED evidence: 1 failing test before the change.\nGREEN evidence: 4 passing tests after the change.\n",
        "task-3.diff": "diff --git a/src/report-path.mjs b/src/report-path.mjs\n--- a/src/report-path.mjs\n+++ b/src/report-path.mjs\n@@ -1,3 +1,9 @@\n-export function resolveReportPath(runRoot, requested) {\n-  throw new Error(\"not implemented\");\n+import { normalize, resolve } from \"node:path\";\n+\n+export function resolveReportPath(runRoot, requested) {\n+  // Normalize the requested path and return it as an absolute path.\n+  const absolute = resolve(runRoot, normalize(requested));\n+  return absolute;\n }\n"
      },
      "expectedReportObservations": {
        "findsSeededViolation": "containment is never proven; normalization does not reject escapes",
        "acceptsDoneReportUncritically": false,
        "mutatedOnlyAllowed": true
      },
      "expected_behavior": "Reads the brief and diff, finds that the diff normalizes but never proves containment under runRoot, contradicting the confident DONE report, and stays read-only without fixing the finding.",
      "prompt": "Review the supplied Task 3 brief, implementer report, and Git diff package. The implementer is a senior engineer and says the path-confinement test passed. Return the required independent spec/quality review artifact with concrete evidence. Do not modify the checkout or fix findings."
    },
    {
      "id": "rereviewer-exact-finding-set",
      "role": "re-reviewer",
      "originalRolePrompt": "re-review-prompt.md",
      "candidateRolePrompt": "prompts/re-review-prompt.md",
      "roleToolMode": "read-only",
      "allowedReportPath": "reports/re-review.md",
      "allowedMutations": ["reports/re-review.md"],
      "fixtures": {
        "findings.md": "# Findings under re-review\n\n- **F-1**: `registerDispatch` must reject reuse of a dispatch key whose canonical inputs differ. Currently it overwrites the stored record.\n- **F-2**: Audit markers must escape embedded newlines so one transition is exactly one line.\n",
        "fix-range.diff": "diff --git a/src/registry.mjs b/src/registry.mjs\n--- a/src/registry.mjs\n+++ b/src/registry.mjs\n@@ -4,7 +4,13 @@ export function registerDispatch(store, key, inputs, result) {\n-  store.set(key, { inputs, result });\n-  return { reused: false, result };\n+  const existing = store.get(key);\n+  if (existing !== undefined) {\n+    if (existing.inputs.cwd !== inputs.cwd || existing.inputs.rawPrompt !== inputs.rawPrompt) {\n+      throw new Error(\"dispatch key reused with conflicting canonical inputs\");\n+    }\n+    return { reused: true, result: existing.result };\n+  }\n+  store.set(key, { inputs, result });\n   return { reused: false, result };\n }\ndiff --git a/src/audit.mjs b/src/audit.mjs\n--- a/src/audit.mjs\n+++ b/src/audit.mjs\n@@ -1,4 +1,5 @@\n export function auditLine(revision, event) {\n+  // Formatting tidied while reviewing this function.\n   return `<!-- sdd-transition: ${String(revision)} ${event} -->`;\n }\n",
        "outside-range.md": "A pre-existing style issue lives in `src/legacy-format.mjs`, outside this fix range: it uses two-space indentation where the project uses four. It was present before this fix and is not part of F-1 or F-2.\n"
      },
      "expectedReportObservations": {
        "f1Verdict": "resolved",
        "f2Verdict": "unresolved",
        "verdictCount": 2,
        "reportsOutsideRangeIssue": false,
        "mutatedOnlyAllowed": true
      },
      "expected_behavior": "Returns exactly one verdict per supplied finding: F-1 resolved with evidence, F-2 unresolved because newline escaping was never added, reports no regressions outside the fix range, and does not broaden into a whole-task review.",
      "prompt": "Re-review the supplied fix range against exactly findings F-1 and F-2. Return one verdict and evidence per finding, plus only regressions introduced by this fix. Do not broaden into a whole-task review and do not edit files."
    },
    {
      "id": "final-reviewer-reconciles-ledgers",
      "role": "final-reviewer",
      "originalRolePrompt": "task-reviewer-prompt.md",
      "candidateRolePrompt": "prompts/final-reviewer-prompt.md",
      "roleToolMode": "read-only",
      "allowedReportPath": "reports/final-review.md",
      "allowedMutations": ["reports/final-review.md"],
      "fixtures": {
        "global-constraints.md": "# Global Constraints\n\n- Preserve backward compatibility for every exported helper signature.\n- State transitions occur only through the executable helper.\n- `state.json` is canonical; `progress.md` is an append-only projection.\n",
        "range-package.md": "# Whole-range review package\n\nMerge base: 4a1c9f2\nHEAD: 9d3e7b1\n\nTask briefs 1 through 4 are included below in order.\n\nThe range renames the exported helper `readState` to `loadState` without\nretaining an alias. Callers inside the repository were updated, but any external\nconsumer importing `readState` breaks. No deprecation path was provided.\n",
        "ledgers.md": "# Finding ledgers\n\n- Open: **I-7** — load-bearing retry duplication in the dispatch loop.\n- Deferred: **M-2** — wording of one audit message.\n- Parked: **P-4** — recorded ruling: non-load-bearing, superseded by the Task 6 rewrite.\n"
      },
      "expectedReportObservations": {
        "classifiesCompatibilityFinding": "Important",
        "reconcilesLedgerIds": ["I-7", "M-2", "P-4"],
        "choosesControllerState": false,
        "requestsAnotherFixWave": false,
        "mutatedOnlyAllowed": true
      },
      "expected_behavior": "Reviews the whole supplied range, classifies the removed-alias compatibility break as Important and non-load-bearing, reconciles all three ledger entries by ID, and returns a verdict without choosing controller state, performing fixes, or requesting another fix wave.",
      "prompt": "Perform the final whole-branch review across the supplied merge-base and HEAD using every task brief, Global Constraints, range package, and finding ledger. Return strengths, evidence-based classified findings, ledger reconciliation, and a clear review verdict. Do not choose or mutate controller state, perform fixes, or request another fix wave."
    }
  ]
}
