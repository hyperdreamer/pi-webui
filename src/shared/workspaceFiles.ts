export const MAX_IMAGE_PREVIEW_BYTES = 10 * 1024 * 1024;
export const MAX_IMAGE_PREVIEW_LABEL = "10 MB";

/** Maximum complete UTF-8 source size for workspace task files and raw reads. */
export const MAX_WORKSPACE_FILE_BYTES = 512 * 1024;
