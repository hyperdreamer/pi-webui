---
"@hyperdreamer/pi-webui": patch
---

Add persistent drag ordering for selected sessions in the Sessions sidebar, with the selected row's handle placed inside the content area immediately before the menu.
